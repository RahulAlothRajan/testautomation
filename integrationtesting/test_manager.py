"""sample test"""
class 2getthere:
    
    # instance attributes
    def __init__(self, version, config):
        self.version = version
        self.config = config
    
    # instance method
    ***

    def validate_release_zip(self, song):
        return "{} sings {}".format(self.name, song)

    def dance(self):
        return "{} is now dancing".format(self.name)
